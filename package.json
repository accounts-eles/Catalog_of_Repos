{
  "name": "repo-preview-generator",
  "version": "1.0.0",
  "description": "Dependencies for generating social media preview cards and syncing to WooCommerce.",
  "main": "generate_preview_script.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "form-data": "^4.0.0",
    "puppeteer": "^22.15.0",
    "rimraf": "3.0.2",
    "simple-git": "3.24.0"
  }
}
